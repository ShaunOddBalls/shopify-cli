    <input type="hidden" in="ob-product-form-attributes" class="product-selected-variant-id" name="id" data-product-id="{{ product.id }}" value="{{ current_variant.id }}">
    <input type="hidden" name="quantity" value="1" min="1" class="qty-selector product__quantity" style="" id="qty-template--16162885926973__main">
    <input type="hidden" name="attributes[_freeshp]" value="{{ settings.vv_handle }}" />
    <input type="hidden" name="attributes[_market]" value="market-{%  if localization.language.root_url == '/' %}uk{% else %}{{ localization.language.root_url | handle }}{% endif %}" />
    <input type="hidden" name="properties[_co]" value="{{ settings.multibuy_variant }}" />
    
    {% if product.tags contains 'design-perfectly-imperfect' %}
          <input type="hidden" name="properties[_special_discount]" value="{{ product.id }}" />
    {% endif %}

    {% if template == 'product.qb' %}
        <input type="hidden" name="attributes[_quickbuy]" value="A" />
        <input type="hidden" name="properties[_quickbuy]" value="A" />
    {% endif %}
    
    {{ cart_attributes }}
    {% if settings.ab_cohort != blank  %}
    <input type="hidden" name="attributes[_cohort]" value="{{ settings.ab_cohort }}" />
    {% endif %}
    {%  for attribute in cart.attributes %}
    <input type="hidden" id="ca_{{ attribute | first | handle }}_{{ attribute | last | handle }}" />       
    {%  endfor %}
    <!-- fg_campaign_name = {{ settings.fg_campaign_name }} -->
    <!-- fg_campaign_tag = {{ settings.fg_campaign_tag }} -->
    {% if settings.fg_campaign_name != blank %}
        {% if settings.fg_campaign_tag != blank %}
            {%  assign settings_fg_campaign_tag = settings.fg_campaign_tag %}
            {% if product.tags contains settings_fg_campaign_tag %}
                <input type="hidden" name="{{ settings.fg_campaign_name }}" value="true" />
                {% if template.name == 'product.qb' %}
                    {% if template contains 'free-gift'  %}
                    <!-- <input type="hidden" name="properties[_cg]" value="{{ variant.id }}" /> -->
                    <input type="hidden" name="properties[_cg]" value="free product" />
                    {% endif %}
                    {% if template contains 'upsell'  %}
                    <input id="cart-matching-upsell" type="hidden" value="{{ product.type }}" name="properties[_Cart Matching Upsell]">
                    {% endif %}
                    {% if template contains 'subscription'  %}
                    <input id="subscription-page" type="hidden" value="{{ product.id }}" name="properties[_Subscription Page]">
                    {% endif %}
                    {% if template contains 'subscription'  %}
                    <input id="subscription-page" type="hidden" value="{{ product.id }}" name="properties[_Subscription Page]">
                    {% endif %}
                    <!-- fg_campaign_name = {{ settings.fg_campaign_name }} -->
                    <!-- fg_campaign_tag = {{ settings.fg_campaign_tag }} -->
                {% else template.name == 'product' %}
                  {% for attribute in cart.attributes %}
                      <input type="hidden" id="ca_{{ attribute | first | handle }}_{{ attribute | last | handle }}" />       
                  {% endfor %}
                
                {% endif %}
            {% endif %}
        {% endif %}
    {% endif %}